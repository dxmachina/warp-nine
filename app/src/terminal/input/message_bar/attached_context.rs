//! Shared message producers for displaying attached blocks/text context.
//!
// LOCAL FORK: every producer here described context attached to an agent
// conversation, so the module is empty until something else needs it.
